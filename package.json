{
  "name": "shazamme-widgets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shazamme Duda widgets \u2014 one shared core, version-pinned bundles on CloudFront.",
  "scripts": {
    "build": "node build/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build && npm run test && npm run validate"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "engines": {
    "node": ">=18"
  }
}
